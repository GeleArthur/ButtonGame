{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "command": "cmake",
            "type": "shell",
            "args": [".."],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": "build"
        }
    ]
}